FigTree example:

```jsx harmony
<svg>
    <Figtree margins={} width={} height={} tree={} >
        <Nodes shape={} label={} filter={} onHover={} onClick={} attrs={}/>
        <Branches curve={} label={}/>
    </Figtree>
</svg>
```
