Group example:

```jsx
import SVG from "./SVG";
import G from "./G";
import Rectangle from "./Rectangle";
<SVG width={200} height={200}>
	<G transform={"translate(100,100)"}>
		<Rectangle width={60} height = {40} fill="steelblue"/>
	</G>
</SVG>
```
